## Vercel Cloud API uses only the Python standard library.
## Desktop dependencies are listed in requirements-desktop.txt.
# The Vercel Cloud API uses only the Python standard library.
# Desktop dependencies are listed in requirements-desktop.txt.
pycaw; sys_platform == "win32"
